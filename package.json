{
  "name": "zero-api-key",
  "private": true,
  "version": "0.1.0",
  "description": "Personal secret manager desktop app (local encrypted vault + Infisical Cloud)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "check": "tsc --noEmit",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "dist": "tauri build && mkdir -p dist && rm -rf dist/ZeroApiKey.app dist/ZeroApiKey_*_aarch64.dmg && cp -R src-tauri/target/release/bundle/macos/ZeroApiKey.app dist/ && cp src-tauri/target/release/bundle/dmg/ZeroApiKey_*_aarch64.dmg dist/"
  },
  "dependencies": {
    "preact": "^10.25.1",
    "@tauri-apps/api": "^2"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.3",
    "typescript": "~5.6.2",
    "vite": "^6.0.3",
    "@tauri-apps/cli": "^2"
  }
}
